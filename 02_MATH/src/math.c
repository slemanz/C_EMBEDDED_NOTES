#include <stdio.h>
#include "math.h"

int math_add(int a, int b)
{
    return (a+b);
}

int math_sub(int a, int b)
{
    return (a - b);
}

int math_mpy(int a, int b)
{
    return (a * b);
}

int math_div(int a, int b)
{
    return (a / b);
}